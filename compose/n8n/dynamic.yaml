http:
  routers:
    n8n:
      entryPoints:
        - n8n
      rule: Host(`{{ env "N8N_ENDPOINT_URL" }}`)
      service: n8n-service
      tls:
        certResolver: letsencrypt

  services:
    n8n-service:
      loadBalancer:
        servers:
          - url: http://n8n:{{ env "PORT_N8N" }}
