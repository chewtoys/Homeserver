http:
  routers:
    mealie:
      entryPoints:
        - mealie
      rule: Host(`{{ env "IP_ADDRESS" }}`)
      service: mealie-service
      middlewares:
        - sablier-mealie@file

  services:
    mealie-service:
      loadBalancer:
        servers:
          - url: http://mealie:9000